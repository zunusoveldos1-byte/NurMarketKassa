<Window x:Class="NurMarketKassa.Views.ShiftsHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:NurMarketKassa.Views"
        Title="История смен"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="CanResize"
        Background="{DynamicResource BrushWindow}"
        FontFamily="Segoe UI">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ShiftHistoryResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{DynamicResource BrushPanel}"
                BorderBrush="{DynamicResource BrushBorder}" BorderThickness="0,0,0,1"
                Padding="20,0" MouseLeftButtonDown="Header_MouseLeftButtonDown">
            <Grid>
                <TextBlock Text="История смен" FontSize="18" FontWeight="SemiBold"
                           Foreground="{DynamicResource BrushText}" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Content="&#xE921;" Style="{StaticResource ShiftWindowChromeButton}"
                            ToolTip="Свернуть" Command="{Binding MinimizeCommand}"/>
                    <Button Content="&#xE8BB;" Style="{StaticResource ShiftWindowChromeButton}"
                            ToolTip="Закрыть" Command="{Binding CloseCommand}" Margin="4,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="230"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{DynamicResource BrushPanelSoft}"
                    BorderBrush="{DynamicResource BrushBorder}" BorderThickness="0,0,1,0">
                <StackPanel Margin="0,12,0,0">
                    <Button Command="{Binding ShowHistoryCommand}">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource ShiftSidebarButton}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsHistoryPage}" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource BrushAccentSoft}"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE8A5;" FontFamily="Segoe MDL2 Assets" FontSize="15"
                                       Margin="0,0,10,0" VerticalAlignment="Center"/>
                            <TextBlock Text="История смен" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <Button Command="{Binding ShowSummaryCommand}">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource ShiftSidebarButton}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsSummaryPage}" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource BrushAccentSoft}"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE9D2;" FontFamily="Segoe MDL2 Assets" FontSize="15"
                                       Margin="0,0,10,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Общий план" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Border>

            <Grid Grid.Column="1" Background="{DynamicResource BrushWindowAlt}">
                <views:ShiftHistoryView Visibility="{Binding IsHistoryPage, Converter={StaticResource BoolToVis}}"/>
                <views:ShiftSummaryView Visibility="{Binding IsSummaryPage, Converter={StaticResource BoolToVis}}"/>

                <Border Background="#66000000" Panel.ZIndex="20"
                        Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock Text="Загрузка..." Foreground="White" FontSize="18" Margin="0,0,0,12"/>
                        <ProgressBar IsIndeterminate="True" MinWidth="200" Height="8"
                                     Foreground="{DynamicResource BrushAccent}"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Window>
