<Window x:Class="NurMarketKassa.Views.FilterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Фильтр товаров"
        Width="480"
        Height="620"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterOwner"
        FontFamily="Segoe UI">
    <Window.Resources>
        <SolidColorBrush x:Key="FilterBg" Color="#121212"/>
        <SolidColorBrush x:Key="FilterBorder" Color="#333333"/>
        <SolidColorBrush x:Key="FilterBorderFocus" Color="#555555"/>
        <SolidColorBrush x:Key="FilterText" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="FilterTextSoft" Color="#99FFFFFF"/>
        <SolidColorBrush x:Key="FilterInputBg" Color="#1A1A1A"/>
        <SolidColorBrush x:Key="FilterAccent" Color="#FFCC00"/>

        <Style x:Key="FilterLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource FilterTextSoft}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

        <Style x:Key="FilterTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource FilterInputBg}"/>
            <Setter Property="Foreground" Value="{StaticResource FilterText}"/>
            <Setter Property="BorderBrush" Value="{StaticResource FilterBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="CaretBrush" Value="{StaticResource FilterText}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Root"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <!-- Триггер IsKeyboardFocused удалён, чтобы граница не менялась -->
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FilterComboBoxItem" TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#121212"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemRoot"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemRoot" Property="Background" Value="#1F1F1F"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemRoot" Property="Background" Value="#2A2A2A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FilterComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource FilterInputBg}"/>
            <Setter Property="Foreground" Value="{StaticResource FilterText}"/>
            <Setter Property="BorderBrush" Value="{StaticResource FilterBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="MinHeight" Value="42"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource FilterComboBoxItem}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <!-- Здесь жестко задаем BorderBrush, чтобы он никогда не менялся -->
                            <Border x:Name="ComboRoot"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{StaticResource FilterBorder}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="28"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter x:Name="ContentSite"
                                              Grid.Column="0"
                                              Margin="12,8,4,8"
                                              HorizontalAlignment="Left"
                                              VerticalAlignment="Center"
                                              IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"/>
                                    <TextBlock Grid.Column="1"
                                       Text="&#xE70D;"
                                       FontFamily="Segoe MDL2 Assets"
                                       FontSize="10"
                                       Foreground="{StaticResource FilterTextSoft}"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"/>

                                    <!-- Полностью кастомный ToggleButton без эффектов наведения -->
                                    <ToggleButton x:Name="ToggleButton"
                                          Grid.ColumnSpan="2"
                                          Focusable="False"
                                          ClickMode="Press"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border Background="Transparent"/>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>
                                </Grid>
                            </Border>
                            <Popup x:Name="PART_Popup"
                           Placement="Bottom"
                           IsOpen="{TemplateBinding IsDropDownOpen}"
                           AllowsTransparency="True"
                           Focusable="False"
                           PopupAnimation="Slide">
                                <Border Background="#121212"
                                BorderBrush="{StaticResource FilterBorder}"
                                BorderThickness="1"
                                CornerRadius="6"
                                MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                MaxHeight="240">
                                    <ScrollViewer SnapsToDevicePixels="True">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FilterGhostButton" TargetType="Button">
            <Setter Property="Background" Value="#3A3A3A"/>
            <Setter Property="Foreground" Value="{StaticResource FilterText}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10" Padding="14,11">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#4A4A4A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FilterAccentButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource FilterAccent}"/>
            <Setter Property="Foreground" Value="#1A1A1A"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10" Padding="14,11">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FFD633"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource FilterBg}"
            BorderBrush="{StaticResource FilterBorder}"
            BorderThickness="1"
            CornerRadius="14"
            Padding="0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*" MinHeight="0"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="#1A1A1A"
                    BorderBrush="{StaticResource FilterBorder}"
                    BorderThickness="0,0,0,1"
                    CornerRadius="14,14,0,0"
                    Padding="18,14"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <TextBlock Text="Фильтр товаров"
                               FontSize="17"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource FilterText}"
                               VerticalAlignment="Center"/>
                    <Button Content="✕"
                            Width="40"
                            Height="36"
                            HorizontalAlignment="Right"
                            Background="Transparent"
                            Foreground="{StaticResource FilterTextSoft}"
                            BorderThickness="0"
                            FontSize="12"
                            Cursor="Hand"
                            Click="Cancel_Click"
                            Margin="0,0,16,0"/>
                </Grid>
            </Border>

            <ScrollViewer Grid.Row="1"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          Padding="20,16,20,8">
                <StackPanel>
                    <TextBlock Text="Поиск" Style="{StaticResource FilterLabel}"/>
                    <Grid Margin="0,0,0,14">
                        <TextBox x:Name="SearchBox" Style="{StaticResource FilterTextBox}"/>
                        <TextBlock Text="Название / ШК / Артикул..."
                                   Foreground="{StaticResource FilterTextSoft}"
                                   FontSize="14"
                                   Margin="14,0,0,0"
                                   VerticalAlignment="Center"
                                   IsHitTestVisible="False">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>

                    <TextBlock Text="Тип товара" Style="{StaticResource FilterLabel}"/>
                    <ComboBox x:Name="CatalogKindCombo"
                              Style="{StaticResource FilterComboBox}"
                              Margin="0,0,0,14"/>

                    <TextBlock Text="Категория" Style="{StaticResource FilterLabel}"/>
                    <ComboBox x:Name="CategoryCombo"
                              Style="{StaticResource FilterComboBox}"
                              Margin="0,0,0,14"/>

                    <TextBlock Text="Бренд" Style="{StaticResource FilterLabel}"/>
                    <ComboBox x:Name="BrandCombo"
                              Style="{StaticResource FilterComboBox}"
                              Margin="0,0,0,14"/>

                    <TextBlock Text="Диапазон цены (сом)" Style="{StaticResource FilterLabel}"/>
                    <Grid Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="12"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="PriceMinBox" Grid.Column="0" Style="{StaticResource FilterTextBox}"/>
                        <TextBox x:Name="PriceMaxBox" Grid.Column="2" Style="{StaticResource FilterTextBox}"/>
                    </Grid>
                    <Grid Margin="0,0,0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="12"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Цена от" Foreground="{StaticResource FilterTextSoft}" FontSize="11"/>
                        <TextBlock Grid.Column="2" Text="Цена до" Foreground="{StaticResource FilterTextSoft}" FontSize="11"/>
                    </Grid>
                </StackPanel>
            </ScrollViewer>

            <Grid Grid.Row="2" Margin="20,0,20,18">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0"
                        Content="Сбросить"
                        Style="{StaticResource FilterGhostButton}"
                        Click="Reset_Click"/>
                <Button Grid.Column="2"
                        Content="Отмена"
                        Style="{StaticResource FilterGhostButton}"
                        Click="Cancel_Click"/>
                <Button Grid.Column="4"
                        Content="Применить"
                        Style="{StaticResource FilterAccentButton}"
                        Click="Apply_Click"/>
            </Grid>
        </Grid>
    </Border>
</Window>
