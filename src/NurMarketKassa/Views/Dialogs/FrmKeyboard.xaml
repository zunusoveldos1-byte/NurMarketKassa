<Window x:Class="NurMarketKassa.Views.Dialogs.FrmKeyboard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Клавиатура"
        PreviewMouseUp="FrmKeyboard_OnMouseUp"
        Focusable="False"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual"
        ShowInTaskbar="False"
        MaxHeight="510" MaxWidth="2200"
        MinHeight="180" MinWidth="180"
        SizeToContent="WidthAndHeight">

    <Window.Resources>
        <Style x:Key="{x:Type Button}" TargetType="{x:Type Button}">
            <Setter Property="Width" Value="{Binding BaseSize}" />
            <Setter Property="Height" Value="{Binding BaseSize}" />
            <Setter Property="Margin" Value="{Binding ButtonsMargin}" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="FontSize" Value="20" />
            <Setter Property="FontWeight" Value="Normal" />
            <Setter Property="Foreground" Value="#E5E7EB" />
            <Setter Property="Background" Value="#2D2D2D" />
            <Setter Property="BorderBrush" Value="#404040" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border"
                                CornerRadius="8"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="4"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3A3A3A" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.78" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkButtn" TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Foreground" Value="#CBD5E1" />
            <Setter Property="Background" Value="#252525" />
            <Setter Property="BorderBrush" Value="#333333" />
        </Style>

        <Style x:Key="KeyboardPanelToggle" TargetType="{x:Type Button}" BasedOn="{StaticResource DarkButtn}">
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Background" Value="#333333" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>
    </Window.Resources>

    <Border CornerRadius="16"
            Background="#1E1E1E"
            BorderBrush="#333333"
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="22" ShadowDepth="6" Opacity="0.35" Color="#000000"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0"
                  Height="34"
                  Background="#252525"
                  MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"
                           Text="Клавиатура"
                           Foreground="#94A3B8"
                           FontSize="12"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"/>
                <Button Grid.Column="1"
                        Width="28"
                        Height="28"
                        Margin="0,0,6,0"
                        Background="Transparent"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="CloseButton_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="closeBd" Background="{TemplateBinding Background}" CornerRadius="14">
                                <TextBlock Text="✕" FontSize="14" Foreground="#F87171"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="closeBd" Property="Background" Value="#3F1D1D"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>

            <Grid Grid.Row="1" HorizontalAlignment="Stretch" Margin="8,4,8,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0" Margin="4" Visibility="{Binding LettersVisibility}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Style="{StaticResource DarkButtn}" Tag="{Binding Button_TAB.Key}" Width="{Binding Button150Size}">
                            <TextBlock Text="⇥" FontSize="18"/>
                        </Button>
                        <Button Content="{Binding Q_button.Value}" Tag="{Binding Q_button.Key}" />
                        <Button Content="{Binding W_button.Value}" Tag="{Binding W_button.Key}" />
                        <Button Content="{Binding E_button.Value}" Tag="{Binding E_button.Key}" />
                        <Button Content="{Binding R_button.Value}" Tag="{Binding R_button.Key}" />
                        <Button Content="{Binding T_button.Value}" Tag="{Binding T_button.Key}" />
                        <Button Content="{Binding Y_button.Value}" Tag="{Binding Y_button.Key}" />
                        <Button Content="{Binding U_button.Value}" Tag="{Binding U_button.Key}" />
                        <Button Content="{Binding I_button.Value}" Tag="{Binding I_button.Key}" />
                        <Button Content="{Binding O_button.Value}" Tag="{Binding O_button.Key}" />
                        <Button Content="{Binding P_button.Value}" Tag="{Binding P_button.Key}" />
                        <Button Content="{Binding Button_219.Value}" Tag="{Binding Button_219.Key}" />
                        <Button Content="{Binding Button_221.Value}" Tag="{Binding Button_221.Key}" />
                        <Button Content="{Binding Button_220.Value}" Tag="{Binding Button_220.Key}" />
                    </StackPanel>
                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
                        <Grid Width="{Binding Button170Size}">
                            <Button Style="{StaticResource DarkButtn}" Tag="{Binding Button_CAPS.Key}" Width="Auto">
                                <TextBlock Text="⇪" FontSize="20" FontWeight="Bold"/>
                            </Button>
                            <Border CornerRadius="3" Margin="10" Background="#6366F1" Width="6" Height="6"
                                    HorizontalAlignment="Left" VerticalAlignment="Bottom"
                                    Visibility="{Binding CapsIndicatorVisibility}" />
                        </Grid>
                        <Button Content="{Binding A_button.Value}" Tag="{Binding A_button.Key}" />
                        <Button Content="{Binding S_button.Value}" Tag="{Binding S_button.Key}" />
                        <Button Content="{Binding D_button.Value}" Tag="{Binding D_button.Key}" />
                        <Button Content="{Binding F_button.Value}" Tag="{Binding F_button.Key}" />
                        <Button Content="{Binding G_button.Value}" Tag="{Binding G_button.Key}" />
                        <Button Content="{Binding H_button.Value}" Tag="{Binding H_button.Key}" />
                        <Button Content="{Binding J_button.Value}" Tag="{Binding J_button.Key}" />
                        <Button Content="{Binding K_button.Value}" Tag="{Binding K_button.Key}" />
                        <Button Content="{Binding L_button.Value}" Tag="{Binding L_button.Key}" />
                        <Button Content="{Binding Button_186.Value}" Tag="{Binding Button_186.Key}" />
                        <Button Content="{Binding Button_222.Value}" Tag="{Binding Button_222.Key}" />
                        <Button Style="{StaticResource DarkButtn}" Tag="{Binding Button_ENTER.Key}" Width="{Binding Button170Size}">
                            <TextBlock Text="↵" FontSize="22" FontWeight="Bold"/>
                        </Button>
                    </StackPanel>
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
                        <Grid>
                            <Button Style="{StaticResource DarkButtn}" Tag="{Binding Button_LSHIFT.Key}" Width="{Binding Button220Size}">
                                <TextBlock Text="⇧" FontSize="22" FontWeight="Bold"/>
                            </Button>
                            <Border CornerRadius="3" Margin="10" Background="#6366F1" Width="6" Height="6"
                                    HorizontalAlignment="Left" VerticalAlignment="Bottom"
                                    Visibility="{Binding ShiftBorderVisibility}" />
                        </Grid>
                        <Button Content="{Binding Z_button.Value}" Tag="{Binding Z_button.Key}" />
                        <Button Content="{Binding X_button.Value}" Tag="{Binding X_button.Key}" />
                        <Button Content="{Binding C_button.Value}" Tag="{Binding C_button.Key}" />
                        <Button Content="{Binding V_button.Value}" Tag="{Binding V_button.Key}" />
                        <Button Content="{Binding B_button.Value}" Tag="{Binding B_button.Key}" />
                        <Button Content="{Binding N_button.Value}" Tag="{Binding N_button.Key}" />
                        <Button Content="{Binding M_button.Value}" Tag="{Binding M_button.Key}" />
                        <Button Content="{Binding Button_188.Value}" Tag="{Binding Button_188.Key}" />
                        <Button Content="{Binding Button_190.Value}" Tag="{Binding Button_190.Key}" />
                        <Button Content="{Binding Button_191.Value}" Tag="{Binding Button_191.Key}" />
                        <Button Style="{StaticResource DarkButtn}" Tag="{Binding Button_LSHIFT.Key}" Width="{Binding Button220Size}">
                            <TextBlock Text="⇧" FontSize="22" FontWeight="Bold"/>
                        </Button>
                    </StackPanel>
                    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button FontSize="16" Style="{StaticResource DarkButtn}" Tag="lang" Content="{Binding CurrnetLang}" Width="{Binding Button150Size}" />
                        <Button Content="Del" Style="{StaticResource DarkButtn}" FontSize="16" Width="{Binding Button150Size}" Tag="{Binding Button_Delete.Key}" />
                        <Button Content="␣" HorizontalAlignment="Stretch" Width="{Binding ButtonSpaceSize}" Tag="{Binding Button_SPACE.Key}" />
                        <Button Style="{StaticResource DarkButtn}" Tag="{Binding Button_BackSpace.Key}" Width="{Binding Button300Size}">
                            <TextBlock Text="⌫" FontSize="20"/>
                        </Button>
                    </StackPanel>
                </Grid>

                <Grid Grid.Column="1">
                    <Button Content=".." Margin="4,8"
                            Width="{Binding Button050Size}" Height="{Binding Button300Size}"
                            Command="{Binding HideShowLettersCommand}"
                            Style="{StaticResource KeyboardPanelToggle}" />
                </Grid>

                <Grid Grid.Column="2" Margin="4">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <Button Content="7" Grid.Column="0" Tag="{Binding Button_D7.Key}" />
                        <Button Content="8" Grid.Column="1" Tag="{Binding Button_D8.Key}" />
                        <Button Content="9" Grid.Column="2" Tag="{Binding Button_D9.Key}" />
                    </Grid>
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <Button Content="4" Grid.Column="0" Tag="{Binding Button_D4.Key}" />
                        <Button Content="5" Grid.Column="1" Tag="{Binding Button_D5.Key}" />
                        <Button Content="6" Grid.Column="2" Tag="{Binding Button_D6.Key}" />
                    </Grid>
                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <Button Content="1" Grid.Column="0" Tag="{Binding Button_D1.Key}" />
                        <Button Content="2" Grid.Column="1" Tag="{Binding Button_D2.Key}" />
                        <Button Content="3" Grid.Column="2" Tag="{Binding Button_D3.Key}" />
                    </Grid>
                    <Grid Grid.Row="3">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <Button Content="0" Grid.Column="0" Width="{Binding Button200Size}" Tag="{Binding Button_D0.Key}" />
                        <Button Grid.Column="1" Content="{Binding Button_D_DOT.Value}" Tag="{Binding Button_D_DOT.Key}" />
                    </Grid>
                </Grid>

                <StackPanel Grid.Column="3" Orientation="Vertical" Margin="0,8,8,8" Visibility="{Binding NumEnterVisibility}">
                    <Button Style="{StaticResource DarkButtn}" Tag="{Binding Button_BackSpace.Key}">
                        <TextBlock Text="⌫" FontSize="20"/>
                    </Button>
                    <Button Style="{StaticResource DarkButtn}" Tag="{Binding Button_ENTER.Key}" Height="{Binding Button300Size}">
                        <TextBlock Text="↵" FontSize="22" FontWeight="Bold"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
