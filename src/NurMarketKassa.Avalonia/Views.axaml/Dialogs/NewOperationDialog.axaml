<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="NurMarketKassa.AvaloniaHost.Views.Dialogs.NewOperationDialog"
        Title="Новая операция"
        Width="480" SizeToContent="Height"
        MaxHeight="680"
        WindowStartupLocation="CenterOwner"
        CanResize="False"
        FontFamily="Segoe UI"
        SystemDecorations="None"
        TransparencyLevelHint="Transparent"
        Background="Transparent">

  <Window.Styles>
    <Style Selector="Button.btn-primary">
      <Setter Property="Background" Value="#F7D617"/>
      <Setter Property="Foreground" Value="#111827"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="CornerRadius" Value="10"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="HorizontalContentAlignment" Value="Center"/>
      <Setter Property="VerticalContentAlignment" Value="Center"/>
      <Setter Property="Cursor" Value="Hand"/>
    </Style>
    <Style Selector="Button.btn-primary:pointerover /template/ ContentPresenter#PART_ContentPresenter">
      <Setter Property="Background" Value="#FFDF2E"/>
    </Style>
    <Style Selector="Button.btn-secondary">
      <Setter Property="Background" Value="#FFFFFF"/>
      <Setter Property="Foreground" Value="#334155"/>
      <Setter Property="BorderBrush" Value="#CBD5E1"/>
      <Setter Property="BorderThickness" Value="1.5"/>
      <Setter Property="CornerRadius" Value="10"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="HorizontalContentAlignment" Value="Center"/>
      <Setter Property="VerticalContentAlignment" Value="Center"/>
      <Setter Property="Cursor" Value="Hand"/>
    </Style>
    <Style Selector="Button.btn-secondary:pointerover /template/ ContentPresenter#PART_ContentPresenter">
      <Setter Property="Background" Value="#F1F5F9"/>
    </Style>
    <Style Selector="RadioButton.tab-pill">
      <Setter Property="HorizontalAlignment" Value="Stretch"/>
      <Setter Property="HorizontalContentAlignment" Value="Center"/>
      <Setter Property="VerticalContentAlignment" Value="Center"/>
      <Setter Property="Height" Value="42"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <ControlTemplate>
          <Border x:Name="Root"
                  Background="#F1F5F9"
                  CornerRadius="10"
                  BorderBrush="#E2E8F0"
                  BorderThickness="1">
            <ContentPresenter Content="{TemplateBinding Content}"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"/>
          </Border>
        </ControlTemplate>
      </Setter>
    </Style>
    <Style Selector="RadioButton.tab-pill:checked /template/ Border#Root">
      <Setter Property="Background" Value="#F7D617"/>
      <Setter Property="BorderBrush" Value="#EAB308"/>
    </Style>
    <Style Selector="TextBox.field">
      <Setter Property="Height" Value="42"/>
      <Setter Property="FontSize" Value="14"/>
      <Setter Property="CornerRadius" Value="10"/>
      <Setter Property="Padding" Value="12,0"/>
      <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>
  </Window.Styles>

  <Border Margin="16" CornerRadius="16"
          Background="#FFFFFF"
          BorderBrush="#E2E8F0" BorderThickness="1"
          BoxShadow="0 12 32 0 #33101828"
          Padding="24">
    <DockPanel LastChildFill="True">

      <StackPanel DockPanel.Dock="Top" Spacing="4" Margin="0,0,0,18">
        <TextBlock Text="Новая операция"
                   FontSize="20" FontWeight="Bold"
                   Foreground="#0F172A"/>
        <TextBlock Text="Внесение или изъятие денег из денежного ящика"
                   FontSize="13" Foreground="#64748B"/>
      </StackPanel>

      <Grid DockPanel.Dock="Bottom" ColumnDefinitions="*,12,*" Margin="0,20,0,0">
        <Button Grid.Column="0"
                Classes="btn-secondary"
                Content="Отмена"
                Height="46"
                IsCancel="True"
                Click="Close_Click"/>
        <Button Grid.Column="2"
                Classes="btn-primary"
                Content="Сохранить"
                Height="46"
                IsDefault="True"
                Click="Save_Click"/>
      </Grid>

      <ScrollViewer VerticalScrollBarVisibility="Auto"
                    MaxHeight="420"
                    HorizontalScrollBarVisibility="Disabled">
        <StackPanel Spacing="12">
          <Grid ColumnDefinitions="*,8,*">
            <RadioButton x:Name="DepositTab"
                         Grid.Column="0"
                         Classes="tab-pill"
                         GroupName="OpType"
                         Content="Внесение"
                         IsChecked="True"/>
            <RadioButton x:Name="WithdrawTab"
                         Grid.Column="2"
                         Classes="tab-pill"
                         GroupName="OpType"
                         Content="Изъятие"/>
          </Grid>

          <StackPanel Spacing="6">
            <TextBlock Text="Дата" FontSize="13" FontWeight="SemiBold" Foreground="#64748B"/>
            <CalendarDatePicker x:Name="DateBox"
                               Height="42"
                               HorizontalAlignment="Stretch"/>
          </StackPanel>

          <StackPanel Spacing="6">
            <TextBlock Text="Кассир" FontSize="13" FontWeight="SemiBold" Foreground="#64748B"/>
            <ComboBox x:Name="CashierBox"
                      Height="42"
                      HorizontalAlignment="Stretch"
                      CornerRadius="10"/>
          </StackPanel>

          <StackPanel Spacing="6">
            <TextBlock Text="Сумма" FontSize="13" FontWeight="SemiBold" Foreground="#64748B"/>
            <Grid>
              <TextBox x:Name="AmountBox"
                       Classes="field"
                       Watermark="0.00"/>
              <TextBlock Text="сом"
                         HorizontalAlignment="Right"
                         VerticalAlignment="Center"
                         Margin="0,0,14,0"
                         Foreground="#64748B"
                         IsHitTestVisible="False"/>
            </Grid>
          </StackPanel>

          <StackPanel Spacing="6">
            <TextBlock Text="Причина" FontSize="13" FontWeight="SemiBold" Foreground="#64748B"/>
            <TextBox x:Name="ReasonBox" Classes="field" Watermark="Например: размен"/>
          </StackPanel>

          <StackPanel Spacing="6">
            <TextBlock Text="Комментарий (необязательно)" FontSize="13" FontWeight="SemiBold" Foreground="#64748B"/>
            <TextBox x:Name="CommentBox"
                     MinHeight="72"
                     FontSize="14"
                     CornerRadius="10"
                     Padding="12,10"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     Watermark="Дополнительная информация"/>
          </StackPanel>
        </StackPanel>
      </ScrollViewer>
    </DockPanel>
  </Border>
</Window>
